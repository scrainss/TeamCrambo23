<!-- Views/Pokemon/Index.cshtml -->
@model List<Pokemon>

<h2>Pokemon List</h2>

<ul>
    @foreach (var pokemon in Model)
    {
        <li>
            @Html.ActionLink(pokemon.Name, "Details", new { id = pokemon.Id })
        </li>
    }
</ul>
